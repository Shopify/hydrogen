containers:
  default:
    build:
      from: ubuntu-latest
      type: ci

# Documentation: https://shopify-build.docs.shopify.io/jobs/publish_package/
steps:
  - label: Publish package
    timeout: 10m
    run:
      - publish:
          type: npm
