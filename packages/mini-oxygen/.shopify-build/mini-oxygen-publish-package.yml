containers:
  default:
    build:
      from: ubuntu-latest
      type: ci

# Documentation: https://shopify-build.docs.shopify.io/jobs/publish_package/
steps:
  - label: Publish package
    timeout: 10m
    run:
      - yarn: ~
      - yarn build
      - publish:
          type: npm
