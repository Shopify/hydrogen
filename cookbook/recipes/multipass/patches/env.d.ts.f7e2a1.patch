diff --git a/templates/skeleton/env.d.ts b/templates/skeleton/env.d.ts
index 000000..111111 100644
--- a/templates/skeleton/env.d.ts
+++ b/templates/skeleton/env.d.ts
@@ -5,3 +5,18 @@
 
 // Enhance TypeScript's built-in typings.
 import '@total-typescript/ts-reset';
+
+declare global {
+  /**
+   * A global `process` object is only available during build to access NODE_ENV.
+   */
+  const process: {env: {NODE_ENV: 'production' | 'development'}};
+
+  /**
+   * Declare expected Env parameter in fetch handler.
+   */
+  interface Env {
+    // @description Multipass secret for Shopify Plus stores
+    PRIVATE_SHOPIFY_STORE_MULTIPASS_SECRET?: string;
+  }
+}