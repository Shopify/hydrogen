---
'@shopify/hydrogen': minor
---

Add a new `@shopify/hydrogen/core` entrypoint that exports the storefront client and its building blocks. `react-router` and `@react-router/dev` are now optional peer dependencies.
