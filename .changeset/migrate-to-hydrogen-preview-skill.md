---
"@shopify/hydrogen": patch
---

Add the `migrate-to-hydrogen-preview` packaged skill. The skill guides an AI agent through the migration of a classic Hydrogen storefront (Remix or React Router) to `@shopify/hydrogen@preview`. Run `hydrogen skills sync` to copy it into your project.
