{
  "private": true,
  "sideEffects": false,
  "scripts": {
    "build": "turbo build --parallel",
    "build:pkg": "npm run build -- --filter=./packages/*",
    "dev": "cross-env LOCAL_DEV=true turbo dev --parallel --filter=!./templates/hello-world",
    "dev:pkg": "npm run dev -- --filter=./packages/*",
    "preview": "turbo build --filter=./packages/* && npm run preview -w demo-store",
    "lint": "eslint --no-error-on-unmatched-pattern --ext .js,.ts,.jsx,.tsx ./packages && npm run lint -w demo-store",
    "format": "prettier --write --ignore-unknown ./packages && npm run format -w demo-store",
    "typecheck": "turbo typecheck --parallel",
    "test": "turbo test",
    "version": "changeset version && node -p \"'export const LIB_VERSION = \\'' + require('./packages/hydrogen/package.json').version + '\\';'\" > packages/hydrogen/src/version.ts",
    "changeset": "changeset",
    "clean-all": "rimraf node_modules/.bin && rimraf node_modules/.cache && rimraf packages/remix-oxygen/dist && rimraf packages/hydrogen/dist && rimraf packages/cli/dist && rimraf templates/demo-store/.cache && rimraf templates/demo-store/.hydrogen"
  },
  "workspaces": [
    "templates/hello-world",
    "templates/demo-store",
    "packages/remix-oxygen",
    "packages/hydrogen",
    "packages/cli"
  ],
  "prettier": "@shopify/prettier-config",
  "devDependencies": {
    "@changesets/changelog-github": "^0.4.7",
    "@changesets/cli": "^2.25.2",
    "@playwright/test": "^1.27.1",
    "@types/eslint": "^8.4.10",
    "cross-env": "^7.0.3",
    "eslint": "^8.20.0",
    "lint-staged": "^10.5.4",
    "prettier": "^2.7.1",
    "rimraf": "^3.0.2",
    "tsup": "^6.5.0",
    "turbo": "^1.6.3",
    "typescript": "^4.8.3",
    "yorkie": "^2.0.0"
  },
  "engines": {
    "node": ">=16.13"
  },
  "gitHooks": {
    "pre-commit": "lint-staged"
  },
  "lint-staged": {
    "*.{js,jsx}": [
      "prettier --write"
    ],
    "*.{ts,tsx}": [
      "eslint",
      "prettier --parser=typescript --write"
    ],
    "*.html": [
      "prettier --write"
    ],
    "*.md": [
      "prettier --write"
    ]
  },
  "dependencies": {
    "fast-array-diff": "^1.0.1"
  }
}
