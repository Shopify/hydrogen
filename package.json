{
  "private": true,
  "sideEffects": false,
  "scripts": {
    "build": "cd templates/demo-store && npm run build",
    "dev": "cd templates/demo-store && npm run dev",
    "preview": "cd templates/demo-store && npm run preview",
    "lint": "eslint --no-error-on-unmatched-pattern --ext .js,.ts,.jsx,.tsx .",
    "format": "prettier --write --ignore-unknown .",
    "typecheck": "tsc --noEmit"
  },
  "workspaces": [
    "templates/demo-store",
    "packages/remix-oxygen",
    "packages/hydrogen",
    "packages/hydrogen-remix",
    "packages/cli"
  ],
  "prettier": "@shopify/prettier-config",
  "devDependencies": {
    "@playwright/test": "^1.27.1",
    "@types/eslint": "^8.4.10",
    "eslint": "^8.20.0",
    "prettier": "^2.7.1",
    "typescript": "^4.8.3"
  },
  "engines": {
    "node": ">=16.13"
  }
}
