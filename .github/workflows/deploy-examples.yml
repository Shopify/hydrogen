# Don't change the line below!
#! oxygen_storefront_id: 1000014888

name: Deploy Recipe Examples
on: 
  push:
    branches: [main]
  pull_request:
    paths:
      - 'templates/skeleton/**'
      - 'cookbook/recipes/**'
      - '.github/workflows/deploy-examples.yml'
  workflow_dispatch: # Allow manual runs

permissions:
  contents: read
  deployments: write

jobs:
  deploy:
    name: Deploy Recipe to Oxygen
    timeout-minutes: 30
    runs-on: ubuntu-latest
    strategy:
      matrix:
        recipes:
          [
            {name: 'metaobjects', token: '1000014928'},
            {name: 'third-party-api', token: '1000014929'},  # Replaces third-party-queries-caching
            {name: 'custom-cart-method', token: '1000014930'},
            {name: 'legacy-customer-account-flow', token: '1000022490'},
          ]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: ⎔ Setup node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'
          cache-dependency-path: '**/package-lock.json'

      - name: Cache node modules
        id: cache-npm
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684 # v4.2.3
        env:
          cache-name: cache-node-modules
        with:
          path: ~/.npm
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.os }}-build-
            ${{ runner.os }}-
      
      - name: 📥 Install dependencies (root)
        run: npm ci
      
      - name: 📦 Build packages
        run: |
          CI=true npm run build:pkg
      
      - name: 📥 Install dependencies (cookbook)
        working-directory: cookbook
        run: npm ci
      
      - name: 🍳 Apply recipe to skeleton
        working-directory: cookbook
        run: |
          echo "Applying recipe: ${{ matrix.recipes.name }}"
          npm run cookbook -- apply --recipe ${{ matrix.recipes.name }}
      
      - name: 📋 Copy skeleton to temporary directory
        run: |
          # Copy skeleton with applied recipe to a temp directory outside the monorepo
          cp -r templates/skeleton /tmp/hydrogen-recipe-${{ matrix.recipes.name }}
          echo "Copied skeleton to /tmp/hydrogen-recipe-${{ matrix.recipes.name }}"
      
      - name: 📥 Install dependencies (skeleton with recipe)
        working-directory: /tmp/hydrogen-recipe-${{ matrix.recipes.name }}
        run: |
          # Use npm install instead of npm ci because recipes may modify package.json
          npm install
      
      - name: 📝 Generate GraphQL types
        run: |
          npx shopify hydrogen codegen --path /tmp/hydrogen-recipe-${{ matrix.recipes.name }}
      
      - name: 🔨 Build skeleton with recipe
        run: |
          npx shopify hydrogen build --path /tmp/hydrogen-recipe-${{ matrix.recipes.name }}
      
      - name: 🚀 Deploy to Oxygen
        id: deploy
        run: |
          npx shopify hydrogen deploy --path /tmp/hydrogen-recipe-${{ matrix.recipes.name }} --no-lockfile-check --token=$OXYGEN_DEPLOYMENT_TOKEN_${{ matrix.recipes.token }}
        env:
          SHOPIFY_HYDROGEN_FLAG_METADATA_DESCRIPTION: "Recipe: ${{ matrix.recipes.name }} - ${{ github.event.head_commit.message }}"
          OXYGEN_DEPLOYMENT_TOKEN_1000014888: ${{ secrets.OXYGEN_DEPLOYMENT_TOKEN_1000014888 }}
          OXYGEN_DEPLOYMENT_TOKEN_1000022490: ${{ secrets.OXYGEN_DEPLOYMENT_TOKEN_1000022490 }}
          OXYGEN_DEPLOYMENT_TOKEN_1000014892: ${{ secrets.OXYGEN_DEPLOYMENT_TOKEN_1000014892 }}
          OXYGEN_DEPLOYMENT_TOKEN_1000014928: ${{ secrets.OXYGEN_DEPLOYMENT_TOKEN_1000014928 }}
          OXYGEN_DEPLOYMENT_TOKEN_1000014929: ${{ secrets.OXYGEN_DEPLOYMENT_TOKEN_1000014929 }}
          OXYGEN_DEPLOYMENT_TOKEN_1000014930: ${{ secrets.OXYGEN_DEPLOYMENT_TOKEN_1000014930 }}
